[project]
name = "riskshield-backend"
version = "0.1.0"
description = "RiskShield AI Abuse-Ring Sentinel Backend API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning",
]
